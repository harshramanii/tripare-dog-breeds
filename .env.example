# Dog Breed Explorer environment variables
#
# The Dog API (v2) is public and requires no key. This file exists so the app
# can be pointed at a mock/proxy in the future without a code change.

# Optional override — leave unset to use https://dogapi.dog/api/v2
# EXPO_PUBLIC_API_BASE_URL=https://dogapi.dog/api/v2
